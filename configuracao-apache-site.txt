Configuracao no DNS
A @ 191.252.92.222 CNAME feo palfmarketing.online


Certificação usando o certbot:
sudo certbot certonly --manual --preferred-challenges=dns -d '*.palfmarketing.online' -m paulofore@gmail.com


Arquivo de Configuracao - copiar de outro:
sudo nano /etc/apache2/sites-available/palfmarketing.online.conf


Registrar o arquivo de configuração:
a2ensite palfmarketing.online.conf

Restart:
systemctl reload apache2
